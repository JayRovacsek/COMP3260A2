extern crate serde_json;

use serde_json::Value as JsonValue;

use std::fs::File;
use std::io::Read;
use std::io::stdin;

fn main() {
    let json_str = r#"
        {
            "key"
        }
    "#;
}